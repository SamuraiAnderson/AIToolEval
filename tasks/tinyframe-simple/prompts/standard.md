Complete the frame parser implementation in the generated repository.

Open `frame_core.c` and implement `FP_AcceptChar` and `FP_Tick` so that parser and timeout behavior satisfy local tests for `frame_config.h`.

Use only local repository files. Do not browse external sources.

Do not change unrelated files unless required for a successful build.
